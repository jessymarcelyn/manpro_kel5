<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Proyek_manpro" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
<!--         <item name="colorPrimary">@color/white</item> -->
    </style>

    <style name="colored_checkbox" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorControlNormal">@color/black</item>  <!-- normal border color change as you wish -->
        <item name="colorControlActivated">@color/selectedNavColor</item> <!-- activated color change as you wish -->
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="colored_input_text" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorControlNormal">@color/white</item>  <!-- normal border color change as you wish -->
        <item name="colorControlActivated">@color/selectedNavColor</item> <!-- activated color change as you wish -->
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.Proyek_manpro" parent="Base.Theme.Proyek_manpro" />

    <style name ="DialogAnimation">
        <item name = "android:windowEnterAnimation">@anim/slide_in_animation</item>
        <item name = "android:windowExitAnimation">@anim/slide_out_animation</item>
    </style>

</resources>